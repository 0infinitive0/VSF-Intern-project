"""Pure domain layer: state, validation, constraints.

Imports nothing above it in the layer stack — no `services`, no I/O, no LLM
client, no Supabase. See `ARCHITECTURE.md` § Layer Architecture & Import Rules.
"""
