# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
# pydantic.EmailStr (CreateVnpayPaymentRequest.guest_email in
# src/models/schemas.py) needs this at import time -- pydantic only checks
# for it lazily, so a machine without it installed crashes on first request
# instead of at startup. Worked in local dev because some other dependency
# happened to pull it in transitively; a clean container build (staging/
# prod) does not have that guarantee.
email-validator>=2.0.0
# fastapi.UploadFile/File (admin hotel image upload,
# src/api/admin/hotels.py::upload_hotel_image) needs this to parse
# multipart/form-data bodies -- same "worked locally via a transitive
# dependency, missing in a clean container" gap as email-validator above.
python-multipart>=0.0.9

# AI / LangChain
langchain>=1.3.0,<2.0.0
langchain-core>=1.5.0,<2.0.0
langchain-openai>=1.4.0,<2.0.0
langchain-ollama>=0.1.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.0.0
langgraph>=1.2.0,<2.0.0

# Database
supabase>=2.31.0,<3.0.0
psycopg2-binary>=2.9.0,<3.0.0

# LangGraph checkpointer (Postgres-backed, coexists with psycopg2 above --
# supabase-py is HTTP-only and psycopg2 is only used by src/airflow/scripts,
# so nothing currently imports psycopg3)
psycopg[binary,pool]>=3.2.0,<4.0.0
langgraph-checkpoint-postgres>=2.0.0,<4.0.0

# Auth (local Supabase JWT verification — no per-request call to Supabase Auth API)
PyJWT[crypto]>=2.9.0,<3.0.0

# Email (booking confirmation — plan 260818-vnpay-payment-and-email-confirmation)
resend>=2.0.0,<3.0.0

# Dev tools
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0

# i18n (gettext catalogs, pybabel tooling)
Babel>=2.15.0
