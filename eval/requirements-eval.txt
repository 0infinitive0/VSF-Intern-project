ragas==0.3.9
rapidfuzz>=3.9          # NonLLMContextPrecisionWithReference needs it
python-dotenv>=1.0.0
langchain-openai>=1.4.0
langchain-ollama>=1.1.0 # embeddings factory imports it
# ragas 0.3.9's llms/base.py unconditionally imports
# langchain_community.chat_models.vertexai, a module langchain-community
# dropped in 0.4.2 (still present in 0.4.1). Pin below the break.
langchain-community<0.4.2
# needed to import backend/src/services/supabase_search.py directly (Phase 3+)
supabase>=2.31.0,<3.0.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
requests>=2.32.0
langsmith>=0.1.0
# needed to import backend/src/agents/session.py directly (Phase 4) - session.py
# transitively pulls in fastapi (src/observability.py), langgraph (src/agents/graph.py),
# and the rest of the backend's own production stack. Same version pins as
# backend/requirements.txt so the two environments agree on behavior; dev-only tools
# (ruff/pytest/httpx) are not needed here.
fastapi>=0.115.0
langchain>=1.3.0,<2.0.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.0.0
langgraph>=1.2.0,<2.0.0
psycopg2-binary>=2.9.0,<3.0.0
Babel>=2.15.0
# needed to import backend/src/api/routes.py directly (Phase 2 of the eval refresh) -
# the e2e harness drives turns through `routes._run_turn_via_graph`, and importing
# that module pulls in `models/schemas.py` (pydantic EmailStr -> email-validator) and
# `services/email_service.py` (resend). Same pins as backend/requirements.txt.
email-validator>=2.0.0
resend>=2.0.0,<3.0.0
